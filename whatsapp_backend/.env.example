# Database provider selection
# DATABASE_PROVIDER can be "SQLite" (default) or "Postgres"
DATABASE_PROVIDER=SQLite

# When using Postgres, set DATABASE_URL like:
# DATABASE_URL=Host=localhost;Database=whatsapp;Username=postgres;Password=postgres
